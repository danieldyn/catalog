name: Test Go Blocking HTTP Server Distroless

on:
  workflow_dispatch:
  push:
    paths:
      - "examples/blocking-httpserver-go1.27-distroless/**"
      - ".github/workflows/test-blocking-httpserver-go1.27-distroless.yml"
  pull_request:
    paths:
      - "examples/blocking-httpserver-go1.27-distroless/**"
      - ".github/workflows/test-blocking-httpserver-go1.27-distroless.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-benchmark-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        # v7.0.1
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Install KraftKit (Version Pinned & Verified)
        env:
          KRAFT_VERSION: "0.12.15"
        run: |
          set -euo pipefail

          wget -q "https://github.com/unikraft/kraftkit/releases/download/v${KRAFT_VERSION}/kraft_${KRAFT_VERSION}_linux_amd64.tar.gz"
          wget -q "https://github.com/unikraft/kraftkit/releases/download/v${KRAFT_VERSION}/kraftkit_${KRAFT_VERSION}_checksums.txt"

          grep " kraft_${KRAFT_VERSION}_linux_amd64.tar.gz\$" "kraftkit_${KRAFT_VERSION}_checksums.txt" | sha256sum -c -

          tar -xzf "kraft_${KRAFT_VERSION}_linux_amd64.tar.gz"
          chmod +x kraft
          sudo install -o root -g root -m 0755 kraft /usr/local/bin/kraft
          kraft version

      - name: Build Unikernel
        working-directory: examples/blocking-httpserver-go1.27-distroless
        run: |
          UK_CFLAGS="-std=gnu17 -Wno-error=incompatible-pointer-types" kraft build --plat qemu --arch x86_64 .

      - name: Measure RootFS Size
        working-directory: examples/blocking-httpserver-go1.27-distroless
        run: |
          set -euo pipefail

          mapfile -t cpio_files < <(find .unikraft/build/ -type f -name "*.cpio")
          if [ "${#cpio_files[@]}" -eq 0 ]; then
            echo "No .cpio artifacts found under .unikraft/build/" >&2
            exit 1
          fi

          du -h "${cpio_files[@]}"

      - name: Build Distroless rootfs for Trivy
        working-directory: examples/blocking-httpserver-go1.27-distroless
        run: docker build --pull -t blocking-httpserver-go1-27-distroless:ci .

      - name: Run Trivy Vulnerability Scanner
        # v0.36.0
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25
        with:
          scan-type: "image"
          scan-ref: "blocking-httpserver-go1-27-distroless:ci"
          format: "table"
          exit-code: "1"
          severity: "CRITICAL,HIGH"

      - name: Install QEMU & Benchmark Tools
        env:
          VEGETA_VERSION: "12.12.0"
        run: |
          set -euo pipefail

          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 wrk

          if [ -e /dev/kvm ]; then
            sudo chown "$(id -u):$(id -g)" /dev/kvm
            sudo chmod 660 /dev/kvm
          fi


          wget -q "https://github.com/tsenart/vegeta/releases/download/v${VEGETA_VERSION}/vegeta_${VEGETA_VERSION}_linux_amd64.tar.gz"
          tar -xzf "vegeta_${VEGETA_VERSION}_linux_amd64.tar.gz"
          chmod +x vegeta
          sudo install -o root -g root -m 0755 vegeta /usr/local/bin/vegeta
          vegeta -version

      - name: Run Unikernel in Background
        working-directory: examples/blocking-httpserver-go1.27-distroless
        run: |
          kraft run -d --plat qemu -p 8080:8080 -M 256M --name httpserver-test .

      - name: Test Network Connectivity
        run: |
          set -euo pipefail

          for i in $(seq 1 15); do
            if curl -s --max-time 2 http://127.0.0.1:8080/ | grep -q "Hello from Unikraft"; then
              echo "Server responded correctly."
              exit 0
            fi
            sleep 1
          done

          echo "Server did not respond as expected within 15s" >&2
          exit 1

      - name: Run Wrk Load Benchmark
        working-directory: examples/blocking-httpserver-go1.27-distroless
        run: bash bench_wrk.sh

      - name: Run Vegeta Load Benchmark
        working-directory: examples/blocking-httpserver-go1.27-distroless
        run: bash bench_vegeta.sh

      - name: Dump Unikernel Logs on Failure
        if: failure()
        working-directory: examples/blocking-httpserver-go1.27-distroless
        run: kraft ps -a; kraft logs httpserver-test || true

      - name: Cleanup
        if: always()
        run: kraft rm --force httpserver-test || true
